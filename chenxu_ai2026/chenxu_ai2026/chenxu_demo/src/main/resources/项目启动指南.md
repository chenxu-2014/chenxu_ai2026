# chenxu_ai2026 项目完整启动指南

> **技术栈**: Spring Boot 3.5.14 + Spring Cloud 2025.0.0 + Spring Cloud Alibaba 2025.0.0.0 + Java 21 + Vue3 + TypeScript

---

## 一、架构概览

```
                         ┌──────────────────┐
                         │   Nacos Server    │  ← 注册中心 + 配置中心（必需）
                         │   :8848           │
                         └────┬─────────────┘
                              │
   ┌──────────────┐    ┌──────┴──────┐    ┌──────────────────┐
   │  Zipkin      │    │             │    │ Sentinel Dashboard│
   │  :9411       │◄───┤  Gateway    │    │ :8858            │
   │  (可选)      │    │  :8080      │    │ (可选)           │
   └──────────────┘    └──────┬──────┘    └──────────────────┘
                              │ 路由转发
                              │
                       ┌──────┴──────┐    ┌──────────────────┐
                       │ chenxu-demo │    │     Kafka        │
                       │ :8081       │◄──►│ :9092            │
                       │             │    │                  │
                       └──────┬──────┘    └──────────────────┘
                              │
                       ┌──────┴──────┐    ┌──────────────────┐
                       │   Redis     │    │     MySQL         │
                       │ :6379       │    │ :3306/austin     │
                       └─────────────┘    └──────────────────┘

    浏览器 / Vue前端(:5173) → Gateway(:8080) → chenxu-demo(:8081)
```

---

## 二、必需中间件（缺一不可）

所有中间件部署在 **192.168.249.129**，用户名密码统一为 `root` / `snCJadmin-1`。

### 2.1 MySQL (3306)

| 项目 | 值 |
|------|-----|
| 地址 | `192.168.249.129:3306` |
| 用户/密码 | `root` / `snCJadmin-1` |
| 数据库 | `austin` |

**需要建 5 张表**：

| 表名 | 建表SQL | 用途 |
|------|---------|------|
| `c_meter` | `chenxu_demo/src/main/resources/c_meter.sql` | 电表数据（1000w 演示） |
| `c_user` | `chenxu_demo/src/main/resources/c_user.sql` | 系统A 用户档案（数据比对源表） |
| `c_cons` | `chenxu_demo/src/main/resources/c_cons.sql` | 系统B 用户档案（数据比对目标表） |
| `c_user_diff` | `chenxu_demo/src/main/resources/c_user_diff.sql` | 差异记录表 |
| `sys_user` | `chenxu-vue/init_sys_user.sql` | Vue 前端登录用户表（新增） |

**建表命令**：
```sql
-- 连接 MySQL
mysql -h 192.168.249.129 -u root -p -D austin

-- 依次执行：
source chenxu_demo/src/main/resources/c_meter.sql
source chenxu_demo/src/main/resources/c_user.sql
source chenxu_demo/src/main/resources/c_cons.sql
source chenxu_demo/src/main/resources/c_user_diff.sql
source chenxu-vue/init_sys_user.sql
```

### 2.2 Nacos (8848)

| 项目 | 值 |
|------|-----|
| 地址 | `http://192.168.249.129:8848/nacos` |
| 用户/密码 | `nacos` / `nacos` |
| 命名空间 | `shiyue`（ID: `589e3087-3772-45e7-95a0-6130a8a038f3`） |

**Nacos 中需要的配置**：

**1) `chenxu-demo.yaml`**（GROUP: DEFAULT_GROUP, 命名空间: shiyue）：
```yaml
spring:
  datasource:
    url: jdbc:mysql://192.168.249.129:3306/austin?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: snCJadmin-1
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.249.129
      port: 6379
      password: snCJadmin-1
      database: 0
      timeout: 5000
  kafka:
    bootstrap-servers: 192.168.249.129:9092
    consumer:
      group-id: cx-hash-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      enable-auto-commit: true
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      properties:
        enable.idempotence: true
        acks: all
        retries: 10
        max.in.flight.requests.per.connection: 5
        batch.size: 16384
        linger.ms: 5

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.redis.load;org.example.datacompare.dto;org.example.datacompare.entity;org.example.auth.entity

kafka:
  topic: HASH_PAR_CX
  partitions: 3

tracing:
  topic: TracingTopic
```

**2) `chenxu-gateway.yaml`**（GROUP: DEFAULT_GROUP, 命名空间: shiyue）：
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: chenxu-demo-route
          uri: lb://chenxu-demo
          predicates:
            - Path=/meter/**,/kafka/**,/redis/**,/sentinel/**,/order/**,/auth/**,/compare/**
          filters:
            - StripPrefix=0
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin
```

### 2.3 Kafka (9092)

| 项目 | 值 |
|------|-----|
| 地址 | `192.168.249.129:9092` |

**需要创建的 Topic**：

| Topic | 分区数 | 用途 |
|-------|--------|------|
| `HASH_PAR_CX` | 3 | Kafka 消息测试 |
| `TracingTopic` | 3 | 交易链路埋点 |

```bash
# 创建 Topic
kafka-topics.sh --create --topic HASH_PAR_CX   --partitions 3 --replication-factor 1 --bootstrap-server 192.168.249.129:9092
kafka-topics.sh --create --topic TracingTopic --partitions 3 --replication-factor 1 --bootstrap-server 192.168.249.129:9092

# 验证
kafka-topics.sh --list --bootstrap-server 192.168.249.129:9092
```

### 2.4 Redis (6379)

| 项目 | 值 |
|------|-----|
| 地址 | `192.168.249.129:6379` |
| 密码 | `snCJadmin-1` |

```bash
# 连通性测试
redis-cli -h 192.168.249.129 -p 6379 -a snCJadmin-1 PING
# 应返回: PONG
```

---

## 三、可选中间件（不影响启动）

| 组件 | 端口 | 用途 | 启动命令 |
|------|------|------|----------|
| **Sentinel Dashboard** | 8858 | 熔断限流控制台 UI | `java -Dserver.port=8858 -jar tools/sentinel-dashboard-1.8.8.jar` |
| **Zipkin Server** | 9411 | 链路追踪可视化 | `java -jar tools/zipkin-server-3.4.4-exec.jar` |

### 3.1 下载与启动（在项目父目录执行）

```bash
# 创建 tools 目录（不会提交到 git，已在 .gitignore 中排除）
mkdir -p tools

# ===== Sentinel Dashboard（~28MB）=====
curl -L -o tools/sentinel-dashboard-1.8.8.jar \
  https://github.com/alibaba/Sentinel/releases/download/1.8.8/sentinel-dashboard-1.8.8.jar

# ===== Zipkin Server（~80MB）=====
curl -L -o tools/zipkin-server-3.4.4-exec.jar \
  https://repo1.maven.org/maven2/io/zipkin/zipkin-server/3.4.4/zipkin-server-3.4.4-exec.jar

# ===== 启动 =====
# 开两个终端分别启动（或者后台运行）
java -Dserver.port=8858 -jar tools/sentinel-dashboard-1.8.8.jar &
java -jar tools/zipkin-server-3.4.4-exec.jar &

# ===== 验证 =====
# Sentinel: http://localhost:8858 （sentinel / sentinel）
# Zipkin:   http://localhost:9411
```

---

## 四、连通性检查脚本

启动前执行，确认所有必需组件在线：

```bash
#!/bin/bash
HOST="192.168.249.129"
RED='\033[0;31m'; GREEN='\033[0;32m'; NC='\033[0m'

check() { nc -z -w3 $HOST $2 2>/dev/null && echo -e "  $1 ($2)  ${GREEN}✓ OK${NC}" || echo -e "  $1 ($2)  ${RED}✗ 不通!${NC}"; }

echo "=== 连通性检查（目标: $HOST）==="
echo "必需组件:"
check "MySQL " 3306
check "Nacos " 8848
check "Kafka " 9092
check "Redis " 6379
echo "可选组件（本地）:"
check "Sentinel(local)" 8858
check "Zipkin  (local)" 9411
```

---

## 五、启动顺序

### 5.0 前置：设置 JDK 21

```bash
# Maven 默认使用 JDK 8，需切换到 JDK 21
export JAVA_HOME="/c/Program Files/Java/jdk-21.0.10"
export PATH="$JAVA_HOME/bin:$PATH"
java -version  # 确认输出 java version "21.x"
```

### 5.1 第一步：启动后端 chenxu_demo（:8081）

```bash
cd chenxu_ai2026/chenxu_ai2026/chenxu_demo
mvn spring-boot:run
```

**启动日志关键行**：
```
[Nacos Config] Loaded config from nacos:chenxu-demo.yaml
[DiscoveryClient] chenxu-demo registered to Nacos
Tomcat started on port 8081
```

### 5.2 第二步：启动网关 chenxu-gateway（:8080，可选）

```bash
cd chenxu_ai2026/chenxu_ai2026/chenxu-gateway
mvn spring-boot:run
```

> 如果不需要网关层统一鉴权和路由，可以跳过此步。Vue 前端已配置 Vite proxy 直连 :8081。

### 5.3 第三步：启动前端 chenxu-vue（:5173）

```bash
cd chenxu_ai2026/chenxu_ai2026/chenxu-vue
npm install        # 首次运行
npm run dev
```

打开浏览器：`http://localhost:5173`

---

## 六、启动验证

### 6.1 后端验证

```bash
# 健康检查
curl http://localhost:8081/actuator/health

# 注册新用户
curl -X POST http://localhost:8081/auth/register \
  -d 'username=admin&password=123456&nickname=管理员'

# 登录获取 Token
curl -X POST http://localhost:8081/auth/login \
  -d 'username=admin&password=123456'
# 返回: {"code":200,"data":{"token":"eyJ...","userId":1,...}}

# 带 Token 访问受保护接口
TOKEN="<上面返回的token>"
curl -H "Authorization: Bearer $TOKEN" http://localhost:8081/meter/count

# 无 Token 应返回 401
curl http://localhost:8081/meter/count
# 返回: {"code":401,"msg":"缺少 Authorization 头或格式错误"}
```

### 6.2 网关验证（如果启动了 gateway）

```bash
# 通过网关访问（端口 8080）
curl http://localhost:8080/meter/count

# 通过网关登录
curl -X POST http://localhost:8080/auth/login \
  -d 'username=admin&password=123456'
```

### 6.3 前端验证

1. 打开 `http://localhost:5173` → 自动跳转到登录页
2. 点击"立即注册" → 输入用户名/密码 → 注册成功
3. 返回登录 → 输入用户名/密码 → 登录 → 进入仪表盘
4. 点击左侧菜单验证各功能模块：
   - **电表数据** → POST 生成 / 加载 / GET 查询
   - **数据比对** → 比对 run / runWithBuckets
   - **Kafka 消息** → 发送消息
   - **Redis 类型** → 9 种数据类型 Demo
   - **熔断限流** → Sentinel 流控/熔断
   - **交易链路** → 下单→库存→支付

---

## 七、模块与中间件依赖关系

```
chenxu_demo (:8081)
  │
  ├── [必需] MySQL   :3306  — 5 张表（c_meter/c_user/c_cons/c_user_diff/sys_user）
  ├── [必需] Nacos   :8848  — 注册发现 + 配置拉取
  ├── [必需] Kafka   :9092  — 消息发送/消费（启动时创建 Topic）
  ├── [必需] Redis   :6379  — 缓存 + 分布式锁 + 数据类型 Demo
  │
  ├── [可选] Sentinel :8858 — 熔断限流 Dashboard（本地规则仍生效）
  └── [可选] Zipkin   :9411 — 链路追踪 UI

chenxu-gateway (:8080)
  │
  ├── [必需] Nacos   :8848  — 注册发现 + 路由配置
  ├── [可选] Sentinel :8858 — 网关层限流 Dashboard
  └── [可选] Zipkin   :9411 — 链路追踪

chenxu-vue (:5173)
  │
  └── Vite proxy → chenxu_demo :8081（直连模式）
      或 Vite proxy → gateway :8080（网关模式）
```

---

## 八、常见问题

### Q1: Maven 编译报 "无效的目标发行版: 21"
```bash
# 确认 JAVA_HOME 指向 JDK 21
export JAVA_HOME="/c/Program Files/Java/jdk-21.0.10"
```

### Q2: 启动报 Nacos 连接超时
- 确认 Nacos Server 在 192.168.249.129:8848 正常运行
- 检查 namespace ID 是否匹配

### Q3: Kafka Topic 不存在
```bash
kafka-topics.sh --create --topic HASH_PAR_CX   --partitions 3 --bootstrap-server 192.168.249.129:9092
kafka-topics.sh --create --topic TracingTopic --partitions 3 --bootstrap-server 192.168.249.129:9092
```

### Q4: 前端 401 / 登录失败
- 确认 MySQL `sys_user` 表已创建
- 确认后端 AuthController 启动正常
- 浏览器 F12 → Application → Local Storage → 清空 token → 重新登录

### Q5: Redis 连接失败
- 确认 Redis 在 192.168.249.129:6379 运行
- 密码是否正确：`snCJadmin-1`

### Q6: 前端跨域报错
- 确认后端 `CorsConfig` 已加载（日志搜索 CorsConfig）
- 或确认 Vite proxy 配置生效（`vite.config.ts` 中配置了所有路径代理）

---

## 九、一键启动脚本（开发用）

```bash
#!/bin/bash
# 保存为 start-all.sh

export JAVA_HOME="/c/Program Files/Java/jdk-21.0.10"

echo "=== 1. 检查连通性 ==="
HOST=192.168.249.129
for port in 3306 8848 9092 6379; do
  nc -z -w2 $HOST $port && echo "  $HOST:$port ✓" || echo "  $HOST:$port ✗ 不通!"
done

echo ""
echo "=== 2. 启动后端 (chenxu_demo :8081) ==="
cd "$(dirname "$0")/chenxu_ai2026/chenxu_demo"
mvn spring-boot:run &
BACKEND_PID=$!
sleep 20

echo ""
echo "=== 3. 启动前端 (chenxu-vue :5173) ==="
cd "$(dirname "$0")/chenxu_ai2026/chenxu-vue"
npm run dev &
FRONTEND_PID=$!

echo ""
echo "=== 启动完成 ==="
echo "后端:  http://localhost:8081"
echo "前端:  http://localhost:5173"
echo ""
echo "Ctrl+C 停止所有服务"
trap "kill $BACKEND_PID $FRONTEND_PID 2>/dev/null; exit" INT TERM
wait
```
